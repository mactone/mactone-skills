# Task Kanban System Dependencies
# 注意：請使用虛擬環境安裝
# python -m venv venv && source venv/bin/activate && pip install -r requirements.txt

flask==3.0.0
bcrypt==4.1.2
python-dotenv==1.0.0
requests==2.31.0
flask-cors==4.0.0
